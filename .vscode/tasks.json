{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Standalone",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target Hex_Standalone",
            "group": "build",
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            },
            "problemMatcher": "$gcc"
        }
    ]
}
